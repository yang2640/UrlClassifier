to install:
pip -r requirements.txt

to run the pipeline:
sh pipeline.sh

three components are separated:
1. web crawling and cache data to "data"
2. extract feature by parsing the crawled data
3. train classifier and make predications


there are lots of todos in TODos